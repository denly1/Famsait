-- Добавляем поле is_pinned для закрепления мероприятия на главной странице
ALTER TABLE events ADD COLUMN IF NOT EXISTS is_pinned BOOLEAN DEFAULT FALSE;

-- Убедиться что только одно событие может быть закреплено (через приложение, не через constraint)
-- При закреплении нового события снимать флаг со всех остальных
